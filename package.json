{
  "name": "openassistant",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "with-env": "bun run --env-file=.env --",
    "with-env:prod": "bun run --env-file=.env.production --",
    "prepare": "effect-language-service patch",
    "typecheck": "bun run --filter '@openassistant/core' typecheck && bun run --filter '@openassistant/reacord' typecheck && bun run --filter '@openassistant/gateway' typecheck && bun run --filter '@openassistant/discord-bot' typecheck",
    "typecheck:tsc": "tsc --noEmit --incremental false -p packages/core/tsconfig.json && tsc --noEmit --incremental false -p apps/gateway/tsconfig.json && tsc --noEmit --incremental false -p apps/discord-bot/tsconfig.json",
    "build": "bun run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "bun run --filter '@openassistant/core' clean && bun run --filter '@openassistant/reacord' clean && bun run --filter '@openassistant/gateway' clean && bun run --filter '@openassistant/discord-bot' clean",
    "dev": "bun run dev:gateway & bun run dev:discord & wait",
    "dev:gateway": "bun run --cwd apps/gateway dev",
    "dev:discord": "bun run --cwd apps/discord-bot dev"
  },
  "devDependencies": {
    "@effect/language-service": "latest",
    "@effect/vitest": "^0.27.0",
    "@types/bun": "latest",
    "@typescript/native-preview": "7.0.0-dev.20260205.1",
    "typescript": "latest",
    "vitest": "^3.2.0"
  },
  "overrides": {
    "@ai-sdk/anthropic": "^2.0.0",
    "@effect/platform": "latest",
    "@effect/platform-bun": "latest",
    "@effect/rpc": "latest",
    "@effect-atom/atom": "^0.4.11",
    "@effect-atom/atom-react": "^0.4.4",
    "@types/react": "^19.0.0",
    "@types/react-reconciler": "^0.31.0",
    "ai": "^6.0.73",
    "discord.js": "^14.17.3",
    "effect": "latest",
    "react": "^19.0.0",
    "react-reconciler": "^0.31.0",
    "zod": "^4.3.6"
  },
  "dependencies": {
    "@apidevtools/swagger-parser": "^12.1.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "yaml": "^2.8.2"
  }
}
